name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build and publish (e.g., v0.1.0)"
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name || inputs.tag }}
      version: ${{ steps.release.outputs.version || inputs.tag }}
      sha: ${{ steps.release.outputs.sha || steps.lookup-release.outputs.sha }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        if: ${{ !inputs.tag }}
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Lookup release target commit
        id: lookup-release
        if: ${{ inputs.tag }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          SHA=$(gh release view "${{ inputs.tag }}" --json targetCommitish -q '.targetCommitish')
          echo "sha=$SHA" >> $GITHUB_OUTPUT
          echo "Found release ${{ inputs.tag }} targeting commit: $SHA"

  publish-release:
    needs: release-please
    if: ${{ needs.release-please.outputs.release_created || inputs.tag }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.release-please.outputs.sha }}

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Verify module
        run: |
          go mod verify
          go build ./...
          go test ./...

      - name: Publish release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          gh release edit ${{ needs.release-please.outputs.tag_name }} --draft=false
